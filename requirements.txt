# Host-side lab tooling for `make load` and `make verify`.
#
# The FastAPI app has its own Docker-only requirements at
# 01-instrument-fastapi/app/requirements.txt and runs on python:3.12-slim.
# Keeping this host file light avoids native builds on Python 3.14.
locust==2.32.4
requests==2.32.3
opentelemetry-api==1.28.2
opentelemetry-sdk==1.28.2
opentelemetry-exporter-otlp==1.28.2
# Scientific dependencies are intentionally kept out of the default install.
# The drift script in this repo is dependency-free for Python 3.14 compatibility.
# Install optional Evidently tooling separately with Python 3.12:
#   python3.12 -m pip install -r requirements-evidently.txt
